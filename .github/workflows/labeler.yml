name: Labeler
on:
  issues:
    types:
      - edited
      - opened

jobs:
  labeler:
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: echo
        run: echo ${{github.rest.issues}}

      # - name: label
      #   uses: actions/github-script@v6
      #   with:
      #     script: |
      #       github.rest.issues.addLabels({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         labels: ["triage"]
      #       })
